const Navbar = () => {
  return (
    <nav className="navbar">
      <div className="links">
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Contact</a>
      </div>
      <h1>Landing</h1>
      <button>Buy Now</button>
    </nav>
  );
};

export default Navbar;
